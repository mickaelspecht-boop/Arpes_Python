# ARPES Explorer — runtime dependencies (Python 3.12)
# Pinned to the majors validated in the development env (micromamba `peaks`).
PyQt6>=6.10,<6.11
numpy>=2.3,<3
scipy>=1.17,<2
matplotlib>=3.10,<4
contourpy>=1.3
pandas>=2.2
openpyxl>=3.1
PyYAML>=6.0
